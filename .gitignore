# Large Natural Earth GeoJSON downloads — regenerated by build/fetch-geo.mjs.
# Not needed at runtime (paths are baked into js/geo-data.js).
data-cache/

# Local Claude Code settings/scratch.
.claude/

# Generated single-file artifact bundle (not used by the GitHub Pages site).
build/se-map-artifact.html
